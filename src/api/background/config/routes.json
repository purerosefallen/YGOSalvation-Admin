{
  "routes": [
    {
      "method": "GET",
      "path": "/backgrounds",
      "handler": "Background.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/backgrounds/count",
      "handler": "Background.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/backgrounds/:_id",
      "handler": "Background.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/backgrounds",
      "handler": "Background.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/backgrounds/:_id",
      "handler": "Background.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/backgrounds/:_id",
      "handler": "Background.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
